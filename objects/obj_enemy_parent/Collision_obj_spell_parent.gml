effect_create_above(ef_explosion, x, y, 1, obj_spell_parent.spell_color);

if (alarm[1 < 0])
{
    hp -= object_index.damage;
    image_blend = c_red;
    
    kb_x = sign(x - other.x);
    kb_y = sign(y - other.y);
    alarm[1] = 20;
}